server:
  port: 8761

spring:
  application:
    name: EurekaServer

eureka:
  server:
    enable-self-preservation: true
  client:
    register-with-eureka: false
    fetch-registry: false

management:
  endpoints:
    web:
      exposure:
        exclude: "*"
