server:
  port: 8761

spring:
  application:
    name: EUREKA-SERVER

eureka:
  server:
    enable-self-preservation: true
  client:
    register-with-eureka: false
    fetch-registry: false

management:
  endpoints:
    web:
      exposure:
        exclude: "*"
